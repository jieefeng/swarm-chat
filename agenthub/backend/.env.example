# AgentHub 环境变量配置

# Anthropic API Key (必需)
ANTHROPIC_API_KEY=your_api_key_here

# API Key for client authentication (可选)
API_KEY=dev-secret-key

# Redis 配置（可选，不配置则使用内存存储）
REDIS_URL=redis://localhost:6379
STORAGE_BACKEND=memory
MESSAGE_TTL_DAYS=30
MAX_MESSAGES=1000